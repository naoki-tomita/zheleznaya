{
  "name": "zheleznaya",
  "version": "0.0.5",
  "main": "index.js",
  "type": "index.d.ts",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.8.2"
  },
  "scripts": {
    "build": "tsc",
    "lint": "npx prettier --write '{**,.}/*.ts'",
    "prepublish": "yarn lint && yarn build"
  }
}
