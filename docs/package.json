{
  "name": "docs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/marked": "^1.1.0",
    "marked": "^1.2.0",
    "parcel": "^1.12.4",
    "zheleznaya": "latest"
  },
  "scripts": {
    "update": "yarn add ../",
    "dev": "parcel index.html",
    "build": "rm -rf dist && NODE_ENV=production parcel build index.html --public-url ./"
  }
}
